"""Nadi simulation package (Phase 0 spine).

Note: ``run_sim.py`` is executed as a script (``python python/src/run_sim.py``), so the
modules here use plain sibling imports rather than package-relative imports.
"""
